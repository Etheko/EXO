// Global UI configuration values

export const DEFAULT_ASCII_WIDTH = 30; // characters (10-100 advisable) 

export const TERMINAL_TOP_SCROLL_COOLDOWN_MS = 1500; // Adjust the delay before top scroll can trigger view change 